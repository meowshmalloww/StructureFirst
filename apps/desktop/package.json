{
  "name": "@structurefirst/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build && electron dist/main.js",
    "start": "electron dist/main.js"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "typescript": "7.0.2"
  }
}
